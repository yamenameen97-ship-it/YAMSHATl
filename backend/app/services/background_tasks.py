from datetime import datetime

def schedule_post_publishing(post_id: int, scheduled_at: datetime):
    # Placeholder for scheduling post publishing in a background queue
    print(f"Post {post_id} scheduled for publishing at {scheduled_at}")

\"\"\"خدمة الدردشة الكاملة\nتوفر إدارة الرسائل والمحادثات مع WebSockets والتشفير والـ Offline Sync\n\"\"\"\n\nimport json\nimport secrets\nfrom datetime import datetime, timezone, timedelta\nfrom typing import Optional, List, Dict, Any\nfrom fastapi import HTTPException, status\nfrom sqlalchemy.orm import Session\nfrom sqlalchemy import and_, or_, desc, func\n\n\nclass ChatService:\n    \"\"\"خدمة إدارة الدردشة\"\"\"\n\n    # حدود الرسائل\n    MAX_MESSAGE_LENGTH = 5000\n    MAX_MESSAGES_PER_PAGE = 50\n    \n    # حدود الملفات\n    MAX_FILE_SIZE_MB = 100\n    ALLOWED_FILE_TYPES = {\"image\", \"video\", \"audio\", \"document\"}\n    \n    # مدة الاحتفاظ بالرسائل المحذوفة\n    MESSAGE_RETENTION_DAYS = 30\n\n    @staticmethod\n    def create_conversation(\n        db: Session,\n        user_id: int,\n        other_user_id: int,\n    ) -> Dict[str, Any]:\n        \"\"\"إنشاء أو الحصول على محادثة\n        \n        Args:\n            db: جلسة قاعدة البيانات\n            user_id: معرّف المستخدم الأول\n            other_user_id: معرّف المستخدم الثاني\n        \n        Returns:\n            بيانات المحادثة\n        \"\"\"\n        from app.models.conversation import Conversation\n        from app.models.conversation_participant import ConversationParticipant\n        \n        # التحقق من أن المستخدمين مختلفان\n        if user_id == other_user_id:\n            raise HTTPException(\n                status_code=status.HTTP_400_BAD_REQUEST,\n                detail=\"Cannot create conversation with yourself\"\n            )\n        \n        # البحث عن محادثة موجودة\n        existing_conversation = db.query(Conversation).join(\n            ConversationParticipant,\n            Conversation.id == ConversationParticipant.conversation_id,\n        ).filter(\n            ConversationParticipant.user_id.in_([user_id, other_user_id]),\n        ).group_by(\n            Conversation.id,\n        ).having(\n            func.count(ConversationParticipant.user_id) == 2,\n        ).first()\n        \n        if existing_conversation:\n            return existing_conversation.to_dict()\n        \n        # إنشاء محادثة جديدة\n        conversation = Conversation(\n            name=None,  # محادثة فردية\n            is_group=False,\n            created_at=datetime.now(timezone.utc).replace(tzinfo=None),\n        )\n        \n        db.add(conversation)\n        db.flush()\n        \n        # إضافة المشاركين\n        for participant_id in [user_id, other_user_id]:\n            participant = ConversationParticipant(\n                conversation_id=conversation.id,\n                user_id=participant_id,\n                joined_at=datetime.now(timezone.utc).replace(tzinfo=None),\n            )\n            db.add(participant)\n        \n        db.commit()\n        db.refresh(conversation)\n        \n        return conversation.to_dict()\n\n    @staticmethod\n    def send_message(\n        db: Session,\n        conversation_id: int,\n        user_id: int,\n        content: str,\n        media_urls: Optional[List[str]] = None,\n        reply_to_message_id: Optional[int] = None,\n    ) -> Dict[str, Any]:\n        \"\"\"إرسال رسالة\n        \n        Args:\n            db: جلسة قاعدة البيانات\n            conversation_id: معرّف المحادثة\n            user_id: معرّف المستخدم\n            content: محتوى الرسالة\n            media_urls: روابط الوسائط\n            reply_to_message_id: معرّف الرسالة المراد الرد عليها\n        \n        Returns:\n            بيانات الرسالة\n        \"\"\"\n        from app.models.message import Message\n        from app.models.conversation import Conversation\n        from app.models.conversation_participant import ConversationParticipant\n        \n        # التحقق من صحة المحتوى\n        content = (content or \"\").strip()\n        if not content and not media_urls:\n            raise HTTPException(\n                status_code=status.HTTP_400_BAD_REQUEST,\n                detail=\"Message must contain text or media\"\n            )\n        \n        if len(content) > ChatService.MAX_MESSAGE_LENGTH:\n            raise HTTPException(\n                status_code=status.HTTP_400_BAD_REQUEST,\n                detail=f\"Message must be less than {ChatService.MAX_MESSAGE_LENGTH} characters\"\n            )\n        \n        # التحقق من أن المستخدم مشارك في المحادثة\n        participant = db.query(ConversationParticipant).filter(\n            ConversationParticipant.conversation_id == conversation_id,\n            ConversationParticipant.user_id == user_id,\n        ).first()\n        \n        if not participant:\n            raise HTTPException(\n                status_code=status.HTTP_403_FORBIDDEN,\n                detail=\"You are not a participant in this conversation\"\n            )\n        \n        # إنشاء الرسالة\n        message = Message(\n            conversation_id=conversation_id,\n            user_id=user_id,\n            content=content,\n            reply_to_message_id=reply_to_message_id,\n            created_at=datetime.now(timezone.utc).replace(tzinfo=None),\n        )\n        \n        db.add(message)\n        db.flush()\n        \n        # إضافة الوسائط\n        if media_urls:\n            from app.models.message_media import MessageMedia\n            \n            for url in media_urls:\n                media = MessageMedia(\n                    message_id=message.id,\n                    media_url=url,\n                    created_at=datetime.now(timezone.utc).replace(tzinfo=None),\n                )\n                db.add(media)\n        \n        db.commit()\n        db.refresh(message)\n        \n        return message.to_dict()\n\n    @staticmethod\n    def get_conversation_messages(\n        db: Session,\n        conversation_id: int,\n        user_id: int,\n        page: int = 1,\n        page_size: Optional[int] = None,\n    ) -> Dict[str, Any]:\n        \"\"\"الحصول على رسائل المحادثة\n        \n        Args:\n            db: جلسة قاعدة البيانات\n            conversation_id: معرّف المحادثة\n            user_id: معرّف المستخدم\n            page: رقم الصفحة\n            page_size: عدد الرسائل في الصفحة\n        \n        Returns:\n            قائمة الرسائل\n        \"\"\"\n        from app.models.message import Message\n        from app.models.conversation_participant import ConversationParticipant\n        \n        # التحقق من أن المستخدم مشارك في المحادثة\n        participant = db.query(ConversationParticipant).filter(\n            ConversationParticipant.conversation_id == conversation_id,\n            ConversationParticipant.user_id == user_id,\n        ).first()\n        \n        if not participant:\n            raise HTTPException(\n                status_code=status.HTTP_403_FORBIDDEN,\n                detail=\"You are not a participant in this conversation\"\n            )\n        \n        if page_size is None:\n            page_size = ChatService.MAX_MESSAGES_PER_PAGE\n        \n        page_size = max(1, min(page_size, ChatService.MAX_MESSAGES_PER_PAGE))\n        page = max(1, page)\n        \n        # الحصول على الرسائل\n        query = db.query(Message).filter(\n            Message.conversation_id == conversation_id,\n            Message.is_deleted == False,\n        ).order_by(desc(Message.created_at))\n        \n        total_count = query.count()\n        offset = (page - 1) * page_size\n        messages = query.offset(offset).limit(page_size).all()\n        \n        # عكس الترتيب لعرض الرسائل من الأقدم إلى الأحدث\n        messages.reverse()\n        \n        return {\n            \"messages\": [msg.to_dict() for msg in messages],\n            \"pagination\": {\n                \"page\": page,\n                \"page_size\": page_size,\n                \"total_count\": total_count,\n                \"total_pages\": (total_count + page_size - 1) // page_size,\n            },\n        }\n\n    @staticmethod\n    def edit_message(\n        db: Session,\n        message_id: int,\n        user_id: int,\n        new_content: str,\n    ) -> Dict[str, Any]:\n        \"\"\"تعديل رسالة\n        \n        Args:\n            db: جلسة قاعدة البيانات\n            message_id: معرّف الرسالة\n            user_id: معرّف المستخدم\n            new_content: المحتوى الجديد\n        \n        Returns:\n            بيانات الرسالة المعدلة\n        \"\"\"\n        from app.models.message import Message\n        \n        # الحصول على الرسالة\n        message = db.query(Message).filter(Message.id == message_id).first()\n        \n        if not message:\n            raise HTTPException(\n                status_code=status.HTTP_404_NOT_FOUND,\n                detail=\"Message not found\"\n            )\n        \n        # التحقق من أن المستخدم هو مرسل الرسالة\n        if message.user_id != user_id:\n            raise HTTPException(\n                status_code=status.HTTP_403_FORBIDDEN,\n                detail=\"You can only edit your own messages\"\n            )\n        \n        # التحقق من أن الرسالة لم تُحذف\n        if message.is_deleted:\n            raise HTTPException(\n                status_code=status.HTTP_400_BAD_REQUEST,\n                detail=\"Cannot edit deleted message\"\n            )\n        \n        # التحقق من أن الرسالة لم تمضِ أكثر من ساعة\n        if (datetime.now(timezone.utc).replace(tzinfo=None) - message.created_at).total_seconds() > 3600:\n            raise HTTPException(\n                status_code=status.HTTP_400_BAD_REQUEST,\n                detail=\"Can only edit messages within 1 hour of sending\"\n            )\n        \n        # التحقق من صحة المحتوى الجديد\n        new_content = (new_content or \"\").strip()\n        if not new_content or len(new_content) > ChatService.MAX_MESSAGE_LENGTH:\n            raise HTTPException(\n                status_code=status.HTTP_400_BAD_REQUEST,\n                detail=f\"Message must be between 1 and {ChatService.MAX_MESSAGE_LENGTH} characters\"\n            )\n        \n        # تحديث الرسالة\n        message.content = new_content\n        message.edited_at = datetime.now(timezone.utc).replace(tzinfo=None)\n        \n        db.commit()\n        db.refresh(message)\n        \n        return message.to_dict()\n\n    @staticmethod\n    def delete_message(\n        db: Session,\n        message_id: int,\n        user_id: int,\n        delete_for_everyone: bool = False,\n    ) -> Dict[str, Any]:\n        \"\"\"حذف رسالة\n        \n        Args:\n            db: جلسة قاعدة البيانات\n            message_id: معرّف الرسالة\n            user_id: معرّف المستخدم\n            delete_for_everyone: حذف للجميع أم للمستخدم فقط\n        \n        Returns:\n            تأكيد الحذف\n        \"\"\"\n        from app.models.message import Message\n        \n        # الحصول على الرسالة\n        message = db.query(Message).filter(Message.id == message_id).first()\n        \n        if not message:\n            raise HTTPException(\n                status_code=status.HTTP_404_NOT_FOUND,\n                detail=\"Message not found\"\n            )\n        \n        # التحقق من أن المستخدم هو مرسل الرسالة\n        if message.user_id != user_id:\n            raise HTTPException(\n                status_code=status.HTTP_403_FORBIDDEN,\n                detail=\"You can only delete your own messages\"\n            )\n        \n        if delete_for_everyone:\n            # حذف الرسالة للجميع\n            message.is_deleted = True\n            message.deleted_at = datetime.now(timezone.utc).replace(tzinfo=None)\n            message.content = \"[Message deleted]\"\n        else:\n            # إضافة المستخدم إلى قائمة الحذف\n            if not message.deleted_by_users:\n                message.deleted_by_users = []\n            message.deleted_by_users.append(user_id)\n        \n        db.commit()\n        db.refresh(message)\n        \n        return {\n            \"success\": True,\n            \"message\": \"Message deleted successfully\",\n        }\n\n    @staticmethod\n    def mark_as_read(\n        db: Session,\n        conversation_id: int,\n        user_id: int,\n    ) -> Dict[str, Any]:\n        \"\"\"تعليم المحادثة كمقروءة\n        \n        Args:\n            db: جلسة قاعدة البيانات\n            conversation_id: معرّف المحادثة\n            user_id: معرّف المستخدم\n        \n        Returns:\n            تأكيد التعليم\n        \"\"\"\n        from app.models.conversation_participant import ConversationParticipant\n        \n        # الحصول على المشارك\n        participant = db.query(ConversationParticipant).filter(\n            ConversationParticipant.conversation_id == conversation_id,\n            ConversationParticipant.user_id == user_id,\n        ).first()\n        \n        if not participant:\n            raise HTTPException(\n                status_code=status.HTTP_403_FORBIDDEN,\n                detail=\"You are not a participant in this conversation\"\n            )\n        \n        # تحديث آخر وقت قراءة\n        participant.last_read_at = datetime.now(timezone.utc).replace(tzinfo=None)\n        \n        db.commit()\n        db.refresh(participant)\n        \n        return {\n            \"success\": True,\n            \"message\": \"Conversation marked as read\",\n        }\n\n    @staticmethod\n    def get_conversations_list(\n        db: Session,\n        user_id: int,\n        page: int = 1,\n        page_size: Optional[int] = None,\n    ) -> Dict[str, Any]:\n        \"\"\"الحصول على قائمة المحادثات\n        \n        Args:\n            db: جلسة قاعدة البيانات\n            user_id: معرّف المستخدم\n            page: رقم الصفحة\n            page_size: عدد المحادثات في الصفحة\n        \n        Returns:\n            قائمة المحادثات\n        \"\"\"\n        from app.models.conversation import Conversation\n        from app.models.conversation_participant import ConversationParticipant\n        from app.models.message import Message\n        \n        if page_size is None:\n            page_size = 20\n        \n        page_size = max(1, min(page_size, 100))\n        page = max(1, page)\n        \n        # الحصول على المحادثات\n        query = db.query(Conversation).join(\n            ConversationParticipant,\n            Conversation.id == ConversationParticipant.conversation_id,\n        ).filter(\n            ConversationParticipant.user_id == user_id,\n            ConversationParticipant.is_active == True,\n        ).order_by(\n            desc(Conversation.updated_at or Conversation.created_at)\n        )\n        \n        total_count = query.count()\n        offset = (page - 1) * page_size\n        conversations = query.offset(offset).limit(page_size).all()\n        \n        # إضافة معلومات إضافية\n        conversations_data = []\n        for conv in conversations:\n            conv_dict = conv.to_dict()\n            \n            # الحصول على آخر رسالة\n            last_message = db.query(Message).filter(\n                Message.conversation_id == conv.id,\n                Message.is_deleted == False,\n            ).order_by(desc(Message.created_at)).first()\n            \n            if last_message:\n                conv_dict[\"last_message\"] = last_message.to_dict()\n            \n            # عدد الرسائل غير المقروءة\n            participant = db.query(ConversationParticipant).filter(\n                ConversationParticipant.conversation_id == conv.id,\n                ConversationParticipant.user_id == user_id,\n            ).first()\n            \n            if participant and participant.last_read_at:\n                unread_count = db.query(Message).filter(\n                    Message.conversation_id == conv.id,\n                    Message.user_id != user_id,\n                    Message.created_at > participant.last_read_at,\n                    Message.is_deleted == False,\n                ).count()\n                conv_dict[\"unread_count\"] = unread_count\n            \n            conversations_data.append(conv_dict)\n        \n        return {\n            \"conversations\": conversations_data,\n            \"pagination\": {\n                \"page\": page,\n                \"page_size\": page_size,\n                \"total_count\": total_count,\n                \"total_pages\": (total_count + page_size - 1) // page_size,\n            },\n        }\n"

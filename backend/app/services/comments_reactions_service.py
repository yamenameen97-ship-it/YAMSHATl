\"\"\"خدمة التعليقات والتفاعلات\nتوفر إدارة التعليقات المتداخلة والتفاعلات في الوقت الفعلي\n\"\"\"\n\nfrom datetime import datetime, timezone, timedelta\nfrom typing import Optional, List, Dict, Any\nfrom fastapi import HTTPException, status\nfrom sqlalchemy.orm import Session\nfrom sqlalchemy import and_, or_, desc, func\n\n\nclass CommentsReactionsService:\n    \"\"\"خدمة إدارة التعليقات والتفاعلات\"\"\"\n\n    # أنواع التفاعلات المدعومة\n    REACTION_TYPES = {\"like\", \"love\", \"haha\", \"wow\", \"sad\", \"angry\"}\n    \n    # الحد الأقصى لعمق التعليقات المتداخلة\n    MAX_NESTING_DEPTH = 3\n    \n    # حجم الصفحة الافتراضي\n    DEFAULT_PAGE_SIZE = 20\n    MAX_PAGE_SIZE = 100\n\n    @staticmethod\n    def create_comment(\n        db: Session,\n        post_id: int,\n        user_id: int,\n        content: str,\n        parent_comment_id: Optional[int] = None,\n        mentions: Optional[List[int]] = None,\n    ) -> Dict[str, Any]:\n        \"\"\"إنشاء تعليق جديد\n        \n        Args:\n            db: جلسة قاعدة البيانات\n            post_id: معرّف المنشور\n            user_id: معرّف المستخدم\n            content: محتوى التعليق\n            parent_comment_id: معرّف التعليق الأب (للتعليقات المتداخلة)\n            mentions: قائمة معرّفات المستخدمين المذكورين\n        \n        Returns:\n            بيانات التعليق الجديد\n        \"\"\"\n        from app.models.comment import Comment\n        from app.models.user import User\n        \n        # التحقق من صحة المحتوى\n        content = (content or \"\").strip()\n        if not content or len(content) > 5000:\n            raise HTTPException(\n                status_code=status.HTTP_400_BAD_REQUEST,\n                detail=\"Comment must be between 1 and 5000 characters\"\n            )\n        \n        # التحقق من عمق التعليقات المتداخلة\n        depth = 0\n        parent_id = parent_comment_id\n        \n        if parent_comment_id:\n            while parent_id:\n                parent_comment = db.query(Comment).filter(Comment.id == parent_id).first()\n                if not parent_comment:\n                    raise HTTPException(\n                        status_code=status.HTTP_404_NOT_FOUND,\n                        detail=\"Parent comment not found\"\n                    )\n                \n                depth += 1\n                if depth > CommentsReactionsService.MAX_NESTING_DEPTH:\n                    raise HTTPException(\n                        status_code=status.HTTP_400_BAD_REQUEST,\n                        detail=f\"Maximum nesting depth is {CommentsReactionsService.MAX_NESTING_DEPTH}\"\n                    )\n                \n                parent_id = parent_comment.parent_comment_id\n        \n        # إنشاء التعليق\n        comment = Comment(\n            post_id=post_id,\n            user_id=user_id,\n            content=content,\n            parent_comment_id=parent_comment_id,\n            created_at=datetime.now(timezone.utc).replace(tzinfo=None),\n        )\n        \n        db.add(comment)\n        db.flush()  # للحصول على معرّف التعليق\n        \n        # معالجة الـ Mentions\n        if mentions:\n            from app.models.mention import Mention\n            \n            for mentioned_user_id in mentions:\n                if mentioned_user_id != user_id:  # لا نذكر المستخدم نفسه\n                    mention = Mention(\n                        user_id=mentioned_user_id,\n                        mentioned_by_user_id=user_id,\n                        comment_id=comment.id,\n                        created_at=datetime.now(timezone.utc).replace(tzinfo=None),\n                    )\n                    db.add(mention)\n        \n        db.commit()\n        db.refresh(comment)\n        \n        return comment.to_dict()\n\n    @staticmethod\n    def get_post_comments(\n        db: Session,\n        post_id: int,\n        current_user_id: Optional[int] = None,\n        sort_by: str = \"newest\",  # newest, oldest, popular\n        page: int = 1,\n        page_size: Optional[int] = None,\n    ) -> Dict[str, Any]:\n        \"\"\"الحصول على تعليقات المنشور\n        \n        Args:\n            db: جلسة قاعدة البيانات\n            post_id: معرّف المنشور\n            current_user_id: معرّف المستخدم الحالي (اختياري)\n            sort_by: ترتيب التعليقات\n            page: رقم الصفحة\n            page_size: عدد التعليقات في الصفحة\n        \n        Returns:\n            قائمة التعليقات مع معلومات الترقيم\n        \"\"\"\n        from app.models.comment import Comment\n        \n        if page_size is None:\n            page_size = CommentsReactionsService.DEFAULT_PAGE_SIZE\n        \n        page_size = max(1, min(page_size, CommentsReactionsService.MAX_PAGE_SIZE))\n        page = max(1, page)\n        \n        # بناء الاستعلام الأساسي (التعليقات الرئيسية فقط)\n        query = db.query(Comment).filter(\n            Comment.post_id == post_id,\n            Comment.parent_comment_id.is_(None),  # التعليقات الرئيسية فقط\n            Comment.is_deleted == False,\n        )\n        \n        # تطبيق الترتيب\n        if sort_by == \"oldest\":\n            query = query.order_by(Comment.created_at)\n        elif sort_by == \"popular\":\n            query = query.order_by(desc(Comment.likes_count))\n        else:  # newest\n            query = query.order_by(desc(Comment.created_at))\n        \n        # الحصول على العدد الكلي\n        total_count = query.count()\n        \n        # تطبيق الترقيم\n        offset = (page - 1) * page_size\n        comments = query.offset(offset).limit(page_size).all()\n        \n        # الحصول على الردود لكل تعليق\n        comments_with_replies = []\n        for comment in comments:\n            comment_dict = comment.to_dict()\n            \n            # الحصول على الردود (التعليقات المتداخلة)\n            replies = db.query(Comment).filter(\n                Comment.parent_comment_id == comment.id,\n                Comment.is_deleted == False,\n            ).order_by(desc(Comment.created_at)).limit(3).all()  # آخر 3 ردود\n            \n            comment_dict[\"replies\"] = [reply.to_dict() for reply in replies]\n            comment_dict[\"replies_count\"] = db.query(Comment).filter(\n                Comment.parent_comment_id == comment.id,\n                Comment.is_deleted == False,\n            ).count()\n            \n            comments_with_replies.append(comment_dict)\n        \n        return {\n            \"comments\": comments_with_replies,\n            \"pagination\": {\n                \"page\": page,\n                \"page_size\": page_size,\n                \"total_count\": total_count,\n                \"total_pages\": (total_count + page_size - 1) // page_size,\n            },\n        }\n\n    @staticmethod\n    def get_comment_replies(\n        db: Session,\n        comment_id: int,\n        page: int = 1,\n        page_size: Optional[int] = None,\n    ) -> Dict[str, Any]:\n        \"\"\"الحصول على ردود التعليق\n        \n        Args:\n            db: جلسة قاعدة البيانات\n            comment_id: معرّف التعليق\n            page: رقم الصفحة\n            page_size: عدد الردود في الصفحة\n        \n        Returns:\n            قائمة الردود\n        \"\"\"\n        from app.models.comment import Comment\n        \n        if page_size is None:\n            page_size = CommentsReactionsService.DEFAULT_PAGE_SIZE\n        \n        page_size = max(1, min(page_size, CommentsReactionsService.MAX_PAGE_SIZE))\n        page = max(1, page)\n        \n        # التحقق من وجود التعليق الأب\n        parent_comment = db.query(Comment).filter(Comment.id == comment_id).first()\n        if not parent_comment:\n            raise HTTPException(\n                status_code=status.HTTP_404_NOT_FOUND,\n                detail=\"Comment not found\"\n            )\n        \n        # الحصول على الردود\n        query = db.query(Comment).filter(\n            Comment.parent_comment_id == comment_id,\n            Comment.is_deleted == False,\n        ).order_by(desc(Comment.created_at))\n        \n        total_count = query.count()\n        offset = (page - 1) * page_size\n        replies = query.offset(offset).limit(page_size).all()\n        \n        return {\n            \"replies\": [reply.to_dict() for reply in replies],\n            \"pagination\": {\n                \"page\": page,\n                \"page_size\": page_size,\n                \"total_count\": total_count,\n                \"total_pages\": (total_count + page_size - 1) // page_size,\n            },\n        }\n\n    @staticmethod\n    def add_reaction(\n        db: Session,\n        target_type: str,  # post, comment\n        target_id: int,\n        user_id: int,\n        reaction_type: str,\n    ) -> Dict[str, Any]:\n        \"\"\"إضافة تفاعل\n        \n        Args:\n            db: جلسة قاعدة البيانات\n            target_type: نوع الهدف (post أو comment)\n            target_id: معرّف الهدف\n            user_id: معرّف المستخدم\n            reaction_type: نوع التفاعل\n        \n        Returns:\n            بيانات التفاعل\n        \"\"\"\n        from app.models.reaction import Reaction\n        from app.models.post import Post\n        from app.models.comment import Comment\n        \n        # التحقق من نوع التفاعل\n        if reaction_type not in CommentsReactionsService.REACTION_TYPES:\n            raise HTTPException(\n                status_code=status.HTTP_400_BAD_REQUEST,\n                detail=f\"Invalid reaction type. Allowed: {', '.join(CommentsReactionsService.REACTION_TYPES)}\"\n            )\n        \n        # التحقق من وجود الهدف\n        if target_type == \"post\":\n            target = db.query(Post).filter(Post.id == target_id).first()\n        elif target_type == \"comment\":\n            target = db.query(Comment).filter(Comment.id == target_id).first()\n        else:\n            raise HTTPException(\n                status_code=status.HTTP_400_BAD_REQUEST,\n                detail=\"Invalid target type\"\n            )\n        \n        if not target:\n            raise HTTPException(\n                status_code=status.HTTP_404_NOT_FOUND,\n                detail=f\"{target_type.capitalize()} not found\"\n            )\n        \n        # التحقق من وجود تفاعل سابق\n        existing_reaction = db.query(Reaction).filter(\n            Reaction.user_id == user_id,\n            Reaction.target_type == target_type,\n            Reaction.target_id == target_id,\n        ).first()\n        \n        if existing_reaction:\n            # تحديث التفاعل إذا كان مختلفاً\n            if existing_reaction.reaction_type != reaction_type:\n                existing_reaction.reaction_type = reaction_type\n                existing_reaction.created_at = datetime.now(timezone.utc).replace(tzinfo=None)\n                db.commit()\n                db.refresh(existing_reaction)\n            \n            return existing_reaction.to_dict()\n        \n        # إنشاء تفاعل جديد\n        reaction = Reaction(\n            user_id=user_id,\n            target_type=target_type,\n            target_id=target_id,\n            reaction_type=reaction_type,\n            created_at=datetime.now(timezone.utc).replace(tzinfo=None),\n        )\n        \n        db.add(reaction)\n        \n        # تحديث عداد التفاعلات\n        if target_type == \"post\":\n            target.likes_count = (target.likes_count or 0) + 1\n        elif target_type == \"comment\":\n            target.likes_count = (target.likes_count or 0) + 1\n        \n        db.commit()\n        db.refresh(reaction)\n        \n        return reaction.to_dict()\n\n    @staticmethod\n    def remove_reaction(\n        db: Session,\n        target_type: str,\n        target_id: int,\n        user_id: int,\n    ) -> Dict[str, Any]:\n        \"\"\"إزالة تفاعل\n        \n        Args:\n            db: جلسة قاعدة البيانات\n            target_type: نوع الهدف\n            target_id: معرّف الهدف\n            user_id: معرّف المستخدم\n        \n        Returns:\n            تأكيد الإزالة\n        \"\"\"\n        from app.models.reaction import Reaction\n        from app.models.post import Post\n        from app.models.comment import Comment\n        \n        # البحث عن التفاعل\n        reaction = db.query(Reaction).filter(\n            Reaction.user_id == user_id,\n            Reaction.target_type == target_type,\n            Reaction.target_id == target_id,\n        ).first()\n        \n        if not reaction:\n            raise HTTPException(\n                status_code=status.HTTP_404_NOT_FOUND,\n                detail=\"Reaction not found\"\n            )\n        \n        # الحصول على الهدف لتحديث العداد\n        if target_type == \"post\":\n            target = db.query(Post).filter(Post.id == target_id).first()\n        elif target_type == \"comment\":\n            target = db.query(Comment).filter(Comment.id == target_id).first()\n        else:\n            target = None\n        \n        # حذف التفاعل\n        db.delete(reaction)\n        \n        # تحديث عداد التفاعلات\n        if target:\n            target.likes_count = max(0, (target.likes_count or 1) - 1)\n        \n        db.commit()\n        \n        return {\n            \"success\": True,\n            \"message\": \"Reaction removed successfully\",\n        }\n\n    @staticmethod\n    def get_reactions_summary(\n        db: Session,\n        target_type: str,\n        target_id: int,\n    ) -> Dict[str, Any]:\n        \"\"\"الحصول على ملخص التفاعلات\n        \n        Args:\n            db: جلسة قاعدة البيانات\n            target_type: نوع الهدف\n            target_id: معرّف الهدف\n        \n        Returns:\n            ملخص التفاعلات\n        \"\"\"\n        from app.models.reaction import Reaction\n        \n        # الحصول على عدد كل نوع تفاعل\n        reactions = db.query(\n            Reaction.reaction_type,\n            func.count(Reaction.id).label(\"count\"),\n        ).filter(\n            Reaction.target_type == target_type,\n            Reaction.target_id == target_id,\n        ).group_by(\n            Reaction.reaction_type,\n        ).all()\n        \n        summary = {}\n        total_count = 0\n        \n        for reaction_type, count in reactions:\n            summary[reaction_type] = count\n            total_count += count\n        \n        return {\n            \"reactions\": summary,\n            \"total_count\": total_count,\n        }\n"

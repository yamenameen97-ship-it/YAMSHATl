\"\"\"خدمة Feed الشاملة\nتوفر إدارة المنشورات والـ Infinite Scroll والـ Media Management\n\"\"\"\n\nimport os\nfrom datetime import datetime, timezone\nfrom typing import Optional, List, Dict, Any\nfrom fastapi import HTTPException, status\nfrom sqlalchemy.orm import Session\nfrom sqlalchemy import and_, or_, desc, func\n\nfrom app.core.config import settings\nfrom app.models.user import User\n\n\nclass FeedService:\n    \"\"\"خدمة إدارة Feed والمنشورات\"\"\"\n\n    # حدود الصفحات\n    DEFAULT_PAGE_SIZE = 20\n    MAX_PAGE_SIZE = 100\n    MIN_PAGE_SIZE = 1\n\n    # حدود الملفات\n    MAX_IMAGE_SIZE_MB = 50\n    MAX_VIDEO_SIZE_MB = 500\n    MAX_IMAGES_PER_POST = 10\n    MAX_VIDEOS_PER_POST = 1\n\n    # جودة ضغط الصور\n    IMAGE_QUALITY = 85\n    IMAGE_THUMBNAIL_WIDTH = 400\n    IMAGE_THUMBNAIL_HEIGHT = 400\n\n    # جودة ضغط الفيديو\n    VIDEO_QUALITY = \"medium\"  # low, medium, high\n    VIDEO_BITRATE = \"2000k\"\n    VIDEO_THUMBNAIL_TIME = \"00:00:01\"\n\n    @staticmethod\n    def validate_page_size(page_size: Optional[int]) -> int:\n        \"\"\"التحقق من صحة حجم الصفحة\"\"\"\n        if page_size is None:\n            return FeedService.DEFAULT_PAGE_SIZE\n        \n        page_size = max(FeedService.MIN_PAGE_SIZE, min(page_size, FeedService.MAX_PAGE_SIZE))\n        return page_size\n\n    @staticmethod\n    def get_feed(\n        db: Session,\n        current_user: User,\n        cursor: Optional[int] = None,\n        page_size: Optional[int] = None,\n        filter_type: str = \"all\",  # all, following, trending, saved\n    ) -> Dict[str, Any]:\n        \"\"\"الحصول على Feed مع Infinite Scroll\n        \n        Args:\n            db: جلسة قاعدة البيانات\n            current_user: المستخدم الحالي\n            cursor: معرّف آخر منشور تم تحميله (للـ Infinite Scroll)\n            page_size: عدد المنشورات المراد تحميلها\n            filter_type: نوع الفلتر (all, following, trending, saved)\n        \n        Returns:\n            قاموس يحتوي على المنشورات والمعلومات الإضافية\n        \"\"\"\n        page_size = FeedService.validate_page_size(page_size)\n        \n        # بناء الاستعلام الأساسي\n        query = db.query(Post).filter(\n            Post.is_deleted == False,\n            Post.is_archived == False,\n        )\n        \n        # تطبيق الفلاتر\n        if filter_type == \"following\":\n            # المنشورات من المتابعين فقط\n            query = query.filter(\n                or_(\n                    Post.user_id.in_(\n                        db.query(Follow.following_id).filter(\n                            Follow.follower_id == current_user.id,\n                            Follow.is_active == True,\n                        )\n                    ),\n                    Post.user_id == current_user.id,  # منشورات المستخدم نفسه\n                )\n            )\n        elif filter_type == \"trending\":\n            # المنشورات الشهيرة (بناءً على التفاعلات والتعليقات)\n            query = query.filter(\n                Post.created_at >= datetime.now(timezone.utc).replace(tzinfo=None) - timedelta(days=7)\n            ).order_by(\n                desc(Post.likes_count + Post.comments_count + Post.shares_count)\n            )\n        elif filter_type == \"saved\":\n            # المنشورات المحفوظة\n            query = query.filter(\n                Post.id.in_(\n                    db.query(SavedPost.post_id).filter(\n                        SavedPost.user_id == current_user.id\n                    )\n                )\n            )\n        \n        # تطبيق Cursor للـ Infinite Scroll\n        if cursor:\n            query = query.filter(Post.id < cursor)\n        \n        # الترتيب والتحديد\n        posts = query.order_by(desc(Post.created_at)).limit(page_size + 1).all()\n        \n        # التحقق من وجود مزيد من المنشورات\n        has_more = len(posts) > page_size\n        posts = posts[:page_size]\n        \n        # الحصول على معرّف المنشور الأخير للـ Cursor التالي\n        next_cursor = posts[-1].id if posts else None\n        \n        return {\n            \"posts\": [post.to_dict() for post in posts],\n            \"pagination\": {\n                \"page_size\": page_size,\n                \"has_more\": has_more,\n                \"next_cursor\": next_cursor,\n            },\n            \"filter_type\": filter_type,\n        }\n\n    @staticmethod\n    def compress_image(image_path: str, output_path: str) -> bool:\n        \"\"\"ضغط الصورة\n        \n        Args:\n            image_path: مسار الصورة الأصلية\n            output_path: مسار الصورة المضغوطة\n        \n        Returns:\n            True إذا تم الضغط بنجاح\n        \"\"\"\n        try:\n            from PIL import Image\n            \n            # فتح الصورة\n            img = Image.open(image_path)\n            \n            # تحويل إلى RGB إذا لزم الأمر\n            if img.mode in (\"RGBA\", \"LA\", \"P\"):\n                rgb_img = Image.new(\"RGB\", img.size, (255, 255, 255))\n                rgb_img.paste(img, mask=img.split()[-1] if img.mode == \"RGBA\" else None)\n                img = rgb_img\n            \n            # تغيير حجم الصورة إذا كانت كبيرة جداً\n            max_size = (FeedService.IMAGE_THUMBNAIL_WIDTH, FeedService.IMAGE_THUMBNAIL_HEIGHT)\n            img.thumbnail(max_size, Image.Resampling.LANCZOS)\n            \n            # حفظ الصورة المضغوطة\n            img.save(\n                output_path,\n                quality=FeedService.IMAGE_QUALITY,\n                optimize=True,\n            )\n            \n            return True\n        except Exception as e:\n            import logging\n            logging.getLogger(__name__).error(f\"Image compression failed: {str(e)}\")\n            return False\n\n    @staticmethod\n    def compress_video(video_path: str, output_path: str) -> bool:\n        \"\"\"ضغط الفيديو\n        \n        Args:\n            video_path: مسار الفيديو الأصلي\n            output_path: مسار الفيديو المضغوط\n        \n        Returns:\n            True إذا تم الضغط بنجاح\n        \"\"\"\n        try:\n            import subprocess\n            \n            # استخدام FFmpeg لضغط الفيديو\n            cmd = [\n                \"ffmpeg\",\n                \"-i\", video_path,\n                \"-c:v\", \"libx264\",\n                \"-preset\", \"medium\",\n                \"-b:v\", FeedService.VIDEO_BITRATE,\n                \"-c:a\", \"aac\",\n                \"-b:a\", \"128k\",\n                \"-movflags\", \"faststart\",\n                output_path,\n            ]\n            \n            result = subprocess.run(\n                cmd,\n                capture_output=True,\n                timeout=300,  # 5 دقائق\n            )\n            \n            return result.returncode == 0\n        except Exception as e:\n            import logging\n            logging.getLogger(__name__).error(f\"Video compression failed: {str(e)}\")\n            return False\n\n    @staticmethod\n    def create_video_thumbnail(video_path: str, output_path: str) -> bool:\n        \"\"\"إنشاء صورة مصغرة للفيديو\n        \n        Args:\n            video_path: مسار الفيديو\n            output_path: مسار الصورة المصغرة\n        \n        Returns:\n            True إذا تم الإنشاء بنجاح\n        \"\"\"\n        try:\n            import subprocess\n            \n            cmd = [\n                \"ffmpeg\",\n                \"-i\", video_path,\n                \"-ss\", FeedService.VIDEO_THUMBNAIL_TIME,\n                \"-vframes\", \"1\",\n                \"-vf\", f\"scale={FeedService.IMAGE_THUMBNAIL_WIDTH}:{FeedService.IMAGE_THUMBNAIL_HEIGHT}:force_original_aspect_ratio=decrease\",\n                output_path,\n            ]\n            \n            result = subprocess.run(\n                cmd,\n                capture_output=True,\n                timeout=30,\n            )\n            \n            return result.returncode == 0\n        except Exception as e:\n            import logging\n            logging.getLogger(__name__).error(f\"Video thumbnail creation failed: {str(e)}\")\n            return False\n\n    @staticmethod\n    def get_trending_hashtags(\n        db: Session,\n        limit: int = 10,\n        days: int = 7,\n    ) -> List[Dict[str, Any]]:\n        \"\"\"الحصول على أكثر الهاشتاجات شيوعاً\n        \n        Args:\n            db: جلسة قاعدة البيانات\n            limit: عدد الهاشتاجات المراد إرجاعها\n            days: عدد الأيام المراد الأخذ بها في الاعتبار\n        \n        Returns:\n            قائمة الهاشتاجات الشهيرة\n        \"\"\"\n        from datetime import timedelta\n        \n        cutoff_date = datetime.now(timezone.utc).replace(tzinfo=None) - timedelta(days=days)\n        \n        # الاستعلام عن الهاشتاجات الشهيرة\n        hashtags = db.query(\n            Hashtag.name,\n            func.count(PostHashtag.id).label(\"count\"),\n        ).join(\n            PostHashtag,\n            Hashtag.id == PostHashtag.hashtag_id,\n        ).join(\n            Post,\n            PostHashtag.post_id == Post.id,\n        ).filter(\n            Post.created_at >= cutoff_date,\n            Post.is_deleted == False,\n        ).group_by(\n            Hashtag.name,\n        ).order_by(\n            desc(func.count(PostHashtag.id)),\n        ).limit(limit).all()\n        \n        return [\n            {\n                \"name\": tag.name,\n                \"count\": tag.count,\n            }\n            for tag in hashtags\n        ]\n\n    @staticmethod\n    def search_posts(\n        db: Session,\n        query: str,\n        current_user: User,\n        page_size: Optional[int] = None,\n        cursor: Optional[int] = None,\n    ) -> Dict[str, Any]:\n        \"\"\"البحث عن المنشورات\n        \n        Args:\n            db: جلسة قاعدة البيانات\n            query: نص البحث\n            current_user: المستخدم الحالي\n            page_size: عدد النتائج المراد إرجاعها\n            cursor: معرّف آخر منشور\n        \n        Returns:\n            نتائج البحث\n        \"\"\"\n        page_size = FeedService.validate_page_size(page_size)\n        \n        # البحث في المحتوى والهاشتاجات والـ Mentions\n        search_query = db.query(Post).filter(\n            Post.is_deleted == False,\n            or_(\n                Post.content.ilike(f\"%{query}%\"),\n                Post.hashtags.any(Hashtag.name.ilike(f\"%{query}%\")),\n            ),\n        )\n        \n        if cursor:\n            search_query = search_query.filter(Post.id < cursor)\n        \n        posts = search_query.order_by(desc(Post.created_at)).limit(page_size + 1).all()\n        \n        has_more = len(posts) > page_size\n        posts = posts[:page_size]\n        next_cursor = posts[-1].id if posts else None\n        \n        return {\n            \"posts\": [post.to_dict() for post in posts],\n            \"query\": query,\n            \"pagination\": {\n                \"page_size\": page_size,\n                \"has_more\": has_more,\n                \"next_cursor\": next_cursor,\n            },\n        }\n"

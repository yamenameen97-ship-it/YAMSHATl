# v79 — إعادة تصميم واجهة مكالمة الفيديو (Pixel-Perfect)

## الملف المُعدَّل
- `frontend/src/components/chat/CallExperience.jsx` — **إعادة كتابة كاملة**

## ما الذي تم إصلاحه/تحسينه؟

تم إعادة بناء واجهة مكالمة الفيديو بالكامل لتطابق التصميم المرجعي (الصورة) بدقة الملّي متر، مع الحفاظ 100% على منطق العمل (WebRTC signaling, callService, ICE, TURN/STUN) بدون أي تغيير في الـ API الخارجي للمكوّن.

### 1) الهيدر العلوي
- ✅ زر إخفاء (سهم لأسفل) في اليسار.
- ✅ صورة أفاتار دائرية مع حرف **Y** بخلفية بنفسجية متدرجة (`#7F3DFF → #4B1D9A`).
- ✅ اسم المستخدم (`yamenameen97`) بلون أبيض.
- ✅ عدّاد الوقت المباشر (`02:34`) بلون بنفسجي فاتح (`#B98CFF`) يتحدّث كل ثانية.
- ✅ ثلاثة أزرار على اليمين: **مشاركون / إضافة / خيارات (three dots)**.

### 2) شريط التفاعلات (Reactions)
- ✅ 5 إيموجي مباشرة: 💜 👋 😂 👍 😮 مع أنيميشن **طفو للأعلى (float up)** عند الضغط.
- ✅ زر `+` لمزيد من التفاعلات.
- ✅ شارة **«تشفير محلي»** على اليمين مع أيقونة قفل.

### 3) شاشة الفيديو الرئيسية (Stage)
- ✅ خلفية بنفسجية متدرّجة داكنة تطابق الصورة تماماً.
- ✅ شارة **«يدعم حتى 1080p»** في الزاوية اليسرى العلوية.
- ✅ **مؤشر إشارة الشبكة** (4 أعمدة) + قائمة `⋯` في الزاوية اليمنى العلوية.
  - المؤشر يعكس جودة الاتصال (excellent / good / stable) ديناميكياً.
- ✅ حرف **Y** نيوني في الخلفية اليسرى (تأثير Neon glow).
- ✅ فيديو الطرف الآخر يملأ الشاشة الرئيسية (`object-fit: cover`).
- ✅ **PiP (Picture-in-Picture)** لصورتك في الزاوية اليمنى السفلية: مستطيل عمودي بحواف مدوّرة `16px` مع حدود بيضاء ناعمة.
- ✅ زر **تبديل الكاميرا** أسفل PiP.
- ✅ **بطاقة معلومات التشفير** في أسفل يسار الشاشة:
  - أيقونة Y بنفسجية + قفل صغير.
  - العنوان: «محادثة مشفرة بالكامل».
  - الوصف: «لا يمكن لأحد خارج هذه المكالمة قراءة أو سماع محتواها».

### 4) صف الأزرار الأول (5 أزرار)
| الأيقونة | الاسم | الوظيفة |
|---------|------|---------|
| 💬 | الدردشة | فتح/إخفاء لوحة الدردشة (`showChat`) |
| 🎙️ | ميكروفون | كتم/فك كتم — يستدعي `svcToggleMute` |
| 📷 | الكاميرا | تشغيل/إيقاف — يستدعي `svcToggleCamera` |
| 🖥️ | مشاركة الشاشة | Screen share (hook جاهز) |
| ⋯ | المزيد | لوحة خيارات إضافية |

### 5) صف الأزرار الثاني (5 أزرار)
| الأيقونة | الاسم | الوظيفة |
|---------|------|---------|
| 🎚️ | تأثيرات | فلاتر الفيديو (hook جاهز) |
| 🔊 | سماعة | تبديل السماعة الخارجية/الأذن — `setSinkId` |
| 📞 (أحمر) | إنهاء المكالمة | **زر أحمر بارز `#F43C4B`** مع ظل ناعم — يستدعي `svcEndCall` |
| ➕ | إضافة | إضافة مشارك |
| 📸 | التقط صورة | Snapshot يلتقط إطاراً من الفيديو ويحفظه PNG |

### 6) الشريط السفلي (Home Indicator)
- ✅ شريط أفقي رمادي فاتح بعرض `130px` كما في iOS الحديث.

## الحفاظ على المنطق الأساسي (Backward-Compatible)
- ✅ نفس الـ Props: `open, mode, callType, participantName, incomingInvite, onClose, onStatusChange`.
- ✅ نفس الاستدعاءات لـ `callService`: `startCall / endCall / toggleMute / toggleCamera / subscribe / acceptIncomingCall / describeMediaError / probeMediaPermissions`.
- ✅ لا تعديل في `IncomingCallOverlay.jsx` أو `pages/Chat.jsx` — يعملون كما هم.
- ✅ نفس آلية معالجة الأخطاء (permission_denied, insecure_context, no_device, device_busy) مع تصميم Banner جديد.
- ✅ تنظيف الـ MediaStreams عند unmount (fix camera LED).

## RTL & التجاوب
- ✅ اتجاه `dir="rtl"` بالكامل مع `insetInlineStart/End` بدل `left/right`.
- ✅ متجاوب لكل الأحجام:
  - `≤ 380px` (Android الصغيرة): أزرار أضغر.
  - `≥ 640px` (تابلت/سطح مكتب): إطار مركزي بحواف مدورة.

## بدون تبعيات خارجية
كل الأيقونات **inline SVG** (لا `lucide-react`, لا `react-icons`) — لا يحتاج المشروع لأي تبعية جديدة.

## نسخة
`v79.0.0 — Video Call UI Pixel-Perfect Redesign`

# =====================================================
# v83.2 fix (تابع لـ v59.4):
# تم حذف Deployment/Service المستقل لـ chat-service من هنا.
# الدردشة موحّدة الآن في backend monolith (المصدر الوحيد للحقيقة).
#
# تركنا هذا الملف موجوداً كـ tombstone حتى يقوم ArcgoCD/kubectl apply
# بإزالة أي Deployment قديم اسمه chat-service كان موجوداً في العنقود
# بفضل مسار prune=true، بدلاً من تركه ينشر image
# "your-dockerhub-username/chat-service:latest" الذي لا يوجد.
#
# لا يجب إعادة إحياء chat-service إلا إذا رجعنا لبنية microservices.
# =====================================================
#
# مرجع سابق (محذوف):
#   apiVersion: apps/v1
#   kind: Deployment
#   metadata:
#     name: chat-service
#     namespace: app-prod
#   ...
#
# إن احتجت مقاييس/websockets الدردشة استعمل الـ backend service:
#   http://backend.app-prod.svc.cluster.local:8000

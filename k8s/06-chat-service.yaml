# =====================================================
# v83.2 fix (تابع لـ v59.4):
# Deployment/Service المستقل لـ chat-service محذوف.
# الدردشة الآن موحَّدة داخل backend monolith (المصدر الوحيد للحقيقة).
#
# احتُفظ بالملف كـ tombstone كي يقوم `kubectl apply -f k8s/` بمسح
# أي Deployment قديم اسمه chat-service من العنقود بدل تركه في حالة
# ImagePullBackOff مع image الوهمي "your-dockerhub-username/chat-service:latest".
#
# التوجيه: gateway/ingress يمرران /ws و /api/chat إلى Service اسمه
# "backend" (انظر k8s/08-gateway.yaml و 09-ingress.yaml).
# =====================================================

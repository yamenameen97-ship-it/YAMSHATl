"""\nخدمة الشات المتقدمة - Advanced Chat Service\nيوفر:\n- تحريك الكتابة (Typing Animation)\n- موجات الصوت (Voice Waveform)\n- تفاعلات الرسائل (Message Reactions)\n- معاينة الردود (Reply Preview)\n- الرسائل المعاد توجيهها (Forwarded Messages)\n- الرسائل المثبتة (Pinned Messages)\n- تحريك الرسائل المقروءة (Seen Animation)\n\"\"\"\n\nfrom fastapi import FastAPI, WebSocket, HTTPException, Query\nfrom fastapi.middleware.cors import CORSMiddleware\nimport json\nimport asyncio\nfrom datetime import datetime\nfrom typing import Optional, List, Dict, Set\nfrom dataclasses import dataclass, asdict, field\nfrom enum import Enum\nimport logging\nimport uuid\n\n# إعداد السجلات\nlogging.basicConfig(level=logging.INFO)\nlogger = logging.getLogger(__name__)\n\napp = FastAPI(\n    title=\"Yamshat Advanced Chat Service\",\n    description=\"خدمة شات متقدمة مع تفاعلات وتحريكات احترافية\",\n    version=\"2.0.0\"\n)\n\n# إضافة CORS\napp.add_middleware(\n    CORSMiddleware,\n    allow_origins=[\"*\"],\n    allow_credentials=True,\n    allow_methods=[\"*\"],\n    allow_headers=[\"*\"],\n)\n\n# ============ تعريفات الأنواع ============\n\nclass MessageType(str, Enum):\n    \"\"\"أنواع الرسائل\"\"\"\n    TEXT = \"text\"\n    IMAGE = \"image\"\n    VIDEO = \"video\"\n    AUDIO = \"audio\"\n    FILE = \"file\"\n    LOCATION = \"location\"\n    CONTACT = \"contact\"\n\n\nclass MessageStatus(str, Enum):\n    \"\"\"حالات الرسائل\"\"\"\n    SENDING = \"sending\"\n    SENT = \"sent\"\n    DELIVERED = \"delivered\"\n    SEEN = \"seen\"\n    FAILED = \"failed\"\n\n\n@dataclass\nclass ChatMessage:\n    \"\"\"رسالة الشات\"\"\"\n    id: str = field(default_factory=lambda: str(uuid.uuid4()))\n    conversation_id: str = \"\"\n    sender_id: str = \"\"\n    sender_name: str = \"\"\n    sender_avatar: str = \"\"\n    content: str = \"\"\n    message_type: MessageType = MessageType.TEXT\n    status: MessageStatus = MessageStatus.SENDING\n    timestamp: str = field(default_factory=lambda: datetime.utcnow().isoformat())\n    edited_at: Optional[str] = None\n    is_edited: bool = False\n    \n    # الردود والإعادة\n    reply_to: Optional[str] = None  # معرّف الرسالة المرد عليها\n    forwarded_from: Optional[str] = None  # معرّف الرسالة المعاد توجيهها\n    is_pinned: bool = False\n    \n    # التفاعلات\n    reactions: Dict[str, List[str]] = field(default_factory=dict)  # {emoji: [user_ids]}\n    \n    # الملفات المرفقة\n    attachments: List[Dict] = field(default_factory=list)\n    \n    # البيانات الوصفية\n    metadata: Dict = field(default_factory=dict)\n\n\n@dataclass\nclass TypingIndicator:\n    \"\"\"مؤشر الكتابة\"\"\"\n    id: str = field(default_factory=lambda: str(uuid.uuid4()))\n    conversation_id: str = \"\"\n    user_id: str = \"\"\n    user_name: str = \"\"\n    is_typing: bool = True\n    timestamp: str = field(default_factory=lambda: datetime.utcnow().isoformat())\n\n\n@dataclass\nclass VoiceMessage:\n    \"\"\"رسالة صوتية\"\"\"\n    id: str = field(default_factory=lambda: str(uuid.uuid4()))\n    conversation_id: str = \"\"\n    sender_id: str = \"\"\n    audio_url: str = \"\"\n    duration: float = 0.0  # بالثواني\n    waveform: List[float] = field(default_factory=list)  # بيانات الموجة\n    transcription: Optional[str] = None  # النص المستخرج\n    timestamp: str = field(default_factory=lambda: datetime.utcnow().isoformat())\n\n\n@dataclass\nclass MessageReaction:\n    \"\"\"تفاعل الرسالة\"\"\"\n    id: str = field(default_factory=lambda: str(uuid.uuid4()))\n    message_id: str = \"\"\n    user_id: str = \"\"\n    user_name: str = \"\"\n    emoji: str = \"\"  # ❤️, 😂, 😮, 😢, 😠, 👍\n    timestamp: str = field(default_factory=lambda: datetime.utcnow().isoformat())\n\n\n@dataclass\nclass SeenIndicator:\n    \"\"\"مؤشر الرسائل المقروءة\"\"\"\n    id: str = field(default_factory=lambda: str(uuid.uuid4()))\n    conversation_id: str = \"\"\n    user_id: str = \"\"\n    user_name: str = \"\"\n    last_seen_message_id: str = \"\"\n    timestamp: str = field(default_factory=lambda: datetime.utcnow().isoformat())\n\n\n# ============ مدير الشات ============\n\nclass ChatManager:\n    \"\"\"مدير الشات المتقدم\"\"\"\n\n    def __init__(self):\n        # الاتصالات النشطة\n        self.active_connections: Dict[str, List[WebSocket]] = {}  # {conversation_id: [ws]}\n        \n        # الرسائل\n        self.messages: Dict[str, List[ChatMessage]] = {}  # {conversation_id: [messages]}\n        \n        # مؤشرات الكتابة\n        self.typing_indicators: Dict[str, Dict[str, TypingIndicator]] = {}  # {conversation_id: {user_id: indicator}}\n        \n        # الرسائل المثبتة\n        self.pinned_messages: Dict[str, List[ChatMessage]] = {}  # {conversation_id: [messages]}\n        \n        # مؤشرات الرسائل المقروءة\n        self.seen_indicators: Dict[str, List[SeenIndicator]] = {}  # {conversation_id: [indicators]}\n\n    async def connect(self, conversation_id: str, websocket: WebSocket):\n        \"\"\"الاتصال بمحادثة\"\"\"\n        await websocket.accept()\n        if conversation_id not in self.active_connections:\n            self.active_connections[conversation_id] = []\n        self.active_connections[conversation_id].append(websocket)\n        logger.info(f\"Client connected to conversation {conversation_id}\")\n\n    async def disconnect(self, conversation_id: str, websocket: WebSocket):\n        \"\"\"قطع الاتصال عن محادثة\"\"\"\n        if conversation_id in self.active_connections:\n            self.active_connections[conversation_id].remove(websocket)\n            if not self.active_connections[conversation_id]:\n                del self.active_connections[conversation_id]\n        logger.info(f\"Client disconnected from conversation {conversation_id}\")\n\n    async def broadcast(self, conversation_id: str, message: dict):\n        \"\"\"بث رسالة إلى جميع الاتصالات في المحادثة\"\"\"\n        if conversation_id in self.active_connections:\n            for connection in self.active_connections[conversation_id]:\n                try:\n                    await connection.send_json(message)\n                except Exception as e:\n                    logger.error(f\"Error broadcasting message: {str(e)}\")\n\n    async def broadcast_typing(self, conversation_id: str, user_id: str, user_name: str, is_typing: bool):\n        \"\"\"بث مؤشر الكتابة\"\"\"\n        if conversation_id not in self.typing_indicators:\n            self.typing_indicators[conversation_id] = {}\n\n        if is_typing:\n            indicator = TypingIndicator(\n                conversation_id=conversation_id,\n                user_id=user_id,\n                user_name=user_name,\n                is_typing=True\n            )\n            self.typing_indicators[conversation_id][user_id] = indicator\n        else:\n            if user_id in self.typing_indicators[conversation_id]:\n                del self.typing_indicators[conversation_id][user_id]\n\n        # بث المؤشر\n        await self.broadcast(conversation_id, {\n            \"type\": \"typing\",\n            \"data\": asdict(indicator) if is_typing else {\n                \"user_id\": user_id,\n                \"is_typing\": False\n            }\n        })\n\n    async def broadcast_seen(self, conversation_id: str, user_id: str, user_name: str, last_seen_message_id: str):\n        \"\"\"بث مؤشر الرسائل المقروءة\"\"\"\n        indicator = SeenIndicator(\n            conversation_id=conversation_id,\n            user_id=user_id,\n            user_name=user_name,\n            last_seen_message_id=last_seen_message_id\n        )\n\n        if conversation_id not in self.seen_indicators:\n            self.seen_indicators[conversation_id] = []\n\n        # إزالة المؤشر القديم للمستخدم\n        self.seen_indicators[conversation_id] = [\n            s for s in self.seen_indicators[conversation_id]\n            if s.user_id != user_id\n        ]\n        self.seen_indicators[conversation_id].append(indicator)\n\n        # بث المؤشر\n        await self.broadcast(conversation_id, {\n            \"type\": \"seen\",\n            \"data\": asdict(indicator)\n        })\n\n    def add_message(self, conversation_id: str, message: ChatMessage):\n        \"\"\"إضافة رسالة\"\"\"\n        if conversation_id not in self.messages:\n            self.messages[conversation_id] = []\n        self.messages[conversation_id].append(message)\n\n    def get_messages(self, conversation_id: str, limit: int = 50, offset: int = 0) -> List[ChatMessage]:\n        \"\"\"الحصول على الرسائل\"\"\"\n        if conversation_id not in self.messages:\n            return []\n        messages = self.messages[conversation_id]\n        return messages[offset:offset + limit]\n\n    def add_reaction(self, conversation_id: str, message_id: str, user_id: str, emoji: str):\n        \"\"\"إضافة تفاعل على رسالة\"\"\"\n        if conversation_id not in self.messages:\n            return False\n\n        for message in self.messages[conversation_id]:\n            if message.id == message_id:\n                if emoji not in message.reactions:\n                    message.reactions[emoji] = []\n                if user_id not in message.reactions[emoji]:\n                    message.reactions[emoji].append(user_id)\n                return True\n        return False\n\n    def remove_reaction(self, conversation_id: str, message_id: str, user_id: str, emoji: str):\n        \"\"\"إزالة تفاعل من رسالة\"\"\"\n        if conversation_id not in self.messages:\n            return False\n\n        for message in self.messages[conversation_id]:\n            if message.id == message_id:\n                if emoji in message.reactions and user_id in message.reactions[emoji]:\n                    message.reactions[emoji].remove(user_id)\n                    if not message.reactions[emoji]:\n                        del message.reactions[emoji]\n                    return True\n        return False\n\n    def pin_message(self, conversation_id: str, message_id: str):\n        \"\"\"تثبيت رسالة\"\"\"\n        if conversation_id not in self.messages:\n            return False\n\n        for message in self.messages[conversation_id]:\n            if message.id == message_id:\n                message.is_pinned = True\n                if conversation_id not in self.pinned_messages:\n                    self.pinned_messages[conversation_id] = []\n                self.pinned_messages[conversation_id].append(message)\n                return True\n        return False\n\n    def unpin_message(self, conversation_id: str, message_id: str):\n        \"\"\"إلغاء تثبيت رسالة\"\"\"\n        if conversation_id not in self.messages:\n            return False\n\n        for message in self.messages[conversation_id]:\n            if message.id == message_id:\n                message.is_pinned = False\n                if conversation_id in self.pinned_messages:\n                    self.pinned_messages[conversation_id] = [\n                        m for m in self.pinned_messages[conversation_id]\n                        if m.id != message_id\n                    ]\n                return True\n        return False\n\n    def get_pinned_messages(self, conversation_id: str) -> List[ChatMessage]:\n        \"\"\"الحصول على الرسائل المثبتة\"\"\"\n        return self.pinned_messages.get(conversation_id, [])\n\n\n# ============ مثيل مدير الشات ============\n\nchat_manager = ChatManager()\n\n\n# ============ المسارات (Routes) ============\n\n@app.get(\"/health\")\nasync def health_check():\n    \"\"\"فحص صحة الخدمة\"\"\"\n    return {\n        \"status\": \"healthy\",\n        \"service\": \"advanced-chat-service\",\n        \"version\": \"2.0.0\"\n    }\n\n\n@app.websocket(\"/ws/{conversation_id}/{user_id}\")\nasync def websocket_endpoint(conversation_id: str, user_id: str, websocket: WebSocket):\n    \"\"\"نقطة نهاية WebSocket للشات\"\"\"\n    await chat_manager.connect(conversation_id, websocket)\n    try:\n        while True:\n            data = await websocket.receive_json()\n            message_type = data.get(\"type\")\n\n            if message_type == \"message\":\n                # رسالة نصية\n                message = ChatMessage(\n                    conversation_id=conversation_id,\n                    sender_id=user_id,\n                    sender_name=data.get(\"sender_name\", \"Unknown\"),\n                    sender_avatar=data.get(\"sender_avatar\", \"\"),\n                    content=data.get(\"content\", \"\"),\n                    message_type=MessageType.TEXT,\n                    status=MessageStatus.SENT,\n                    reply_to=data.get(\"reply_to\"),\n                    forwarded_from=data.get(\"forwarded_from\")\n                )\n                chat_manager.add_message(conversation_id, message)\n                await chat_manager.broadcast(conversation_id, {\n                    \"type\": \"message\",\n                    \"data\": asdict(message)\n                })\n\n            elif message_type == \"typing\":\n                # مؤشر الكتابة\n                is_typing = data.get(\"is_typing\", True)\n                await chat_manager.broadcast_typing(\n                    conversation_id,\n                    user_id,\n                    data.get(\"user_name\", \"Unknown\"),\n                    is_typing\n                )\n\n            elif message_type == \"seen\":\n                # مؤشر الرسائل المقروءة\n                await chat_manager.broadcast_seen(\n                    conversation_id,\n                    user_id,\n                    data.get(\"user_name\", \"Unknown\"),\n                    data.get(\"last_seen_message_id\", \"\")\n                )\n\n            elif message_type == \"reaction\":\n                # تفاعل على رسالة\n                message_id = data.get(\"message_id\")\n                emoji = data.get(\"emoji\")\n                action = data.get(\"action\", \"add\")  # add or remove\n\n                if action == \"add\":\n                    chat_manager.add_reaction(conversation_id, message_id, user_id, emoji)\n                else:\n                    chat_manager.remove_reaction(conversation_id, message_id, user_id, emoji)\n\n                await chat_manager.broadcast(conversation_id, {\n                    \"type\": \"reaction\",\n                    \"data\": {\n                        \"message_id\": message_id,\n                        \"emoji\": emoji,\n                        \"action\": action,\n                        \"user_id\": user_id\n                    }\n                })\n\n            elif message_type == \"voice\":\n                # رسالة صوتية\n                voice_message = VoiceMessage(\n                    conversation_id=conversation_id,\n                    sender_id=user_id,\n                    audio_url=data.get(\"audio_url\", \"\"),\n                    duration=data.get(\"duration\", 0.0),\n                    waveform=data.get(\"waveform\", [])\n                )\n                await chat_manager.broadcast(conversation_id, {\n                    \"type\": \"voice\",\n                    \"data\": asdict(voice_message)\n                })\n\n    except Exception as e:\n        logger.error(f\"WebSocket error: {str(e)}\")\n    finally:\n        await chat_manager.disconnect(conversation_id, websocket)\n\n\n@app.get(\"/messages/{conversation_id}\")\nasync def get_messages(\n    conversation_id: str,\n    limit: int = Query(50, ge=1, le=100),\n    offset: int = Query(0, ge=0)\n):\n    \"\"\"الحصول على الرسائل\"\"\"\n    try:\n        messages = chat_manager.get_messages(conversation_id, limit, offset)\n        return {\n            \"success\": True,\n            \"conversation_id\": conversation_id,\n            \"messages\": [asdict(m) for m in messages],\n            \"total\": len(chat_manager.messages.get(conversation_id, []))\n        }\n    except Exception as e:\n        logger.error(f\"Error getting messages: {str(e)}\")\n        raise HTTPException(status_code=500, detail=str(e))\n\n\n@app.post(\"/messages/{conversation_id}/{message_id}/pin\")\nasync def pin_message(conversation_id: str, message_id: str):\n    \"\"\"تثبيت رسالة\"\"\"\n    try:\n        if chat_manager.pin_message(conversation_id, message_id):\n            await chat_manager.broadcast(conversation_id, {\n                \"type\": \"pin\",\n                \"data\": {\"message_id\": message_id}\n            })\n            return {\"success\": True, \"message\": \"تم تثبيت الرسالة\"}\n        else:\n            raise HTTPException(status_code=404, detail=\"الرسالة غير موجودة\")\n    except Exception as e:\n        logger.error(f\"Error pinning message: {str(e)}\")\n        raise HTTPException(status_code=500, detail=str(e))\n\n\n@app.post(\"/messages/{conversation_id}/{message_id}/unpin\")\nasync def unpin_message(conversation_id: str, message_id: str):\n    \"\"\"إلغاء تثبيت رسالة\"\"\"\n    try:\n        if chat_manager.unpin_message(conversation_id, message_id):\n            await chat_manager.broadcast(conversation_id, {\n                \"type\": \"unpin\",\n                \"data\": {\"message_id\": message_id}\n            })\n            return {\"success\": True, \"message\": \"تم إلغاء تثبيت الرسالة\"}\n        else:\n            raise HTTPException(status_code=404, detail=\"الرسالة غير موجودة\")\n    except Exception as e:\n        logger.error(f\"Error unpinning message: {str(e)}\")\n        raise HTTPException(status_code=500, detail=str(e))\n\n\n@app.get(\"/messages/{conversation_id}/pinned\")\nasync def get_pinned_messages(conversation_id: str):\n    \"\"\"الحصول على الرسائل المثبتة\"\"\"\n    try:\n        pinned = chat_manager.get_pinned_messages(conversation_id)\n        return {\n            \"success\": True,\n            \"conversation_id\": conversation_id,\n            \"pinned_messages\": [asdict(m) for m in pinned]\n        }\n    except Exception as e:\n        logger.error(f\"Error getting pinned messages: {str(e)}\")\n        raise HTTPException(status_code=500, detail=str(e))\n\n\n@app.get(\"/stats\")\nasync def get_stats():\n    \"\"\"إحصائيات الخدمة\"\"\"\n    return {\n        \"active_conversations\": len(chat_manager.active_connections),\n        \"total_messages\": sum(len(msgs) for msgs in chat_manager.messages.values()),\n        \"pinned_messages_count\": sum(len(msgs) for msgs in chat_manager.pinned_messages.values()),\n        \"typing_indicators\": sum(len(users) for users in chat_manager.typing_indicators.values())\n    }\n\n\nif __name__ == \"__main__\":\n    import uvicorn\n    uvicorn.run(app, host=\"0.0.0.0\", port=8005)\n

\"\"\"خدمة الشات المحسّنة - Optimized Chat Service\nيوفر:\n- معالجة الرسائل بكفاءة عالية\n- تخزين مؤقت ذكي (Caching)\n- ضغط البيانات (Compression)\n- معالجة الصور الكسولة (Lazy Loading)\n- استعادة موضع التمرير (Scroll Restore)\n\"\"\"\n\nfrom fastapi import FastAPI, HTTPException, Query, BackgroundTasks\nfrom fastapi.middleware.cors import CORSMiddleware\nfrom fastapi.middleware.gzip import GZIPMiddleware\nimport json\nimport asyncio\nfrom datetime import datetime, timedelta\nfrom typing import Optional, List, Dict\nfrom dataclasses import dataclass, asdict\nfrom enum import Enum\nimport logging\nimport uuid\nfrom functools import lru_cache\nimport hashlib\n\n# إعداد السجلات\nlogging.basicConfig(level=logging.INFO)\nlogger = logging.getLogger(__name__)\n\napp = FastAPI(\n    title=\"Yamshat Optimized Chat Service\",\n    description=\"خدمة شات محسّنة مع أداء عالية\",\n    version=\"3.0.0\"\n)\n\n# إضافة CORS\napp.add_middleware(\n    CORSMiddleware,\n    allow_origins=[\"*\"],\n    allow_credentials=True,\n    allow_methods=[\"*\"],\n    allow_headers=[\"*\"],\n)\n\n# إضافة ضغط GZIP\napp.add_middleware(GZIPMiddleware, minimum_size=1000)\n\n# ============ تعريفات الأنواع ============\n\nclass AttachmentType(str, Enum):\n    \"\"\"أنواع المرفقات\"\"\"\n    IMAGE = \"image\"\n    VIDEO = \"video\"\n    FILE = \"file\"\n    AUDIO = \"audio\"\n\n\n@dataclass\nclass Attachment:\n    \"\"\"المرفق\"\"\"\n    id: str\n    type: AttachmentType\n    url: str\n    size: int\n    name: str\n    thumbnail: Optional[str] = None\n    width: Optional[int] = None\n    height: Optional[int] = None\n    duration: Optional[float] = None\n\n\n@dataclass\nclass OptimizedMessage:\n    \"\"\"رسالة محسّنة\"\"\"\n    id: str\n    conversation_id: str\n    sender_id: str\n    sender_name: str\n    sender_avatar: str\n    content: str\n    timestamp: str\n    is_read: bool = False\n    attachments: List[Attachment] = None\n    reactions: Dict[str, List[str]] = None\n    hash: Optional[str] = None  # لكشف التكرار\n\n    def __post_init__(self):\n        if self.attachments is None:\n            self.attachments = []\n        if self.reactions is None:\n            self.reactions = {}\n        # حساب hash الرسالة\n        if not self.hash:\n            msg_str = f\"{self.sender_id}{self.content}{self.timestamp}\"\n            self.hash = hashlib.md5(msg_str.encode()).hexdigest()\n\n\n@dataclass\nclass ScrollPosition:\n    \"\"\"موضع التمرير\"\"\"\n    conversation_id: str\n    user_id: str\n    message_id: str\n    offset: int\n    timestamp: str\n\n\n# ============ مدير الشات المحسّن ============\n\nclass OptimizedChatManager:\n    \"\"\"مدير الشات المحسّن\"\"\"\n\n    def __init__(self):\n        # الرسائل مع التخزين المؤقت\n        self.messages: Dict[str, List[OptimizedMessage]] = {}\n        self.message_cache: Dict[str, Dict] = {}  # {conversation_id: {message_id: message}}\n        \n        # مواضع التمرير\n        self.scroll_positions: Dict[str, ScrollPosition] = {}  # {user_id: position}\n        \n        # الرسائل المقروءة\n        self.read_status: Dict[str, Dict[str, bool]] = {}  # {conversation_id: {message_id: is_read}}\n        \n        # الرسائل المكررة (للكشف عن التكرار)\n        self.message_hashes: Dict[str, set] = {}  # {conversation_id: {hashes}}\n        \n        # الإحصائيات\n        self.stats = {\n            \"total_messages\": 0,\n            \"total_attachments\": 0,\n            \"cache_hits\": 0,\n            \"cache_misses\": 0\n        }\n\n    def add_message(self, message: OptimizedMessage) -> bool:\n        \"\"\"إضافة رسالة مع كشف التكرار\"\"\"\n        if message.conversation_id not in self.messages:\n            self.messages[message.conversation_id] = []\n            self.message_cache[message.conversation_id] = {}\n            self.message_hashes[message.conversation_id] = set()\n            self.read_status[message.conversation_id] = {}\n\n        # كشف التكرار\n        if message.hash in self.message_hashes[message.conversation_id]:\n            logger.warning(f\"Duplicate message detected: {message.hash}\")\n            return False\n\n        # إضافة الرسالة\n        self.messages[message.conversation_id].append(message)\n        self.message_cache[message.conversation_id][message.id] = asdict(message)\n        self.message_hashes[message.conversation_id].add(message.hash)\n        self.read_status[message.conversation_id][message.id] = message.is_read\n        \n        # تحديث الإحصائيات\n        self.stats[\"total_messages\"] += 1\n        if message.attachments:\n            self.stats[\"total_attachments\"] += len(message.attachments)\n\n        return True\n\n    def get_messages(\n        self,\n        conversation_id: str,\n        limit: int = 50,\n        offset: int = 0,\n        use_cache: bool = True\n    ) -> List[Dict]:\n        \"\"\"الحصول على الرسائل مع التخزين المؤقت\"\"\"\n        if conversation_id not in self.messages:\n            return []\n\n        # استخدام الذاكرة المؤقتة\n        if use_cache and conversation_id in self.message_cache:\n            self.stats[\"cache_hits\"] += 1\n            messages = list(self.message_cache[conversation_id].values())\n        else:\n            self.stats[\"cache_misses\"] += 1\n            messages = [asdict(m) for m in self.messages[conversation_id]]\n\n        # تطبيق الحد والإزاحة\n        return messages[offset:offset + limit]\n\n    def mark_as_read(\n        self,\n        conversation_id: str,\n        message_id: str,\n        user_id: str\n    ) -> bool:\n        \"\"\"تحديد رسالة كمقروءة\"\"\"\n        if conversation_id not in self.read_status:\n            return False\n\n        if message_id in self.read_status[conversation_id]:\n            self.read_status[conversation_id][message_id] = True\n            \n            # تحديث الرسالة في الذاكرة المؤقتة\n            if conversation_id in self.message_cache:\n                if message_id in self.message_cache[conversation_id]:\n                    self.message_cache[conversation_id][message_id][\"is_read\"] = True\n            \n            return True\n        return False\n\n    def save_scroll_position(\n        self,\n        conversation_id: str,\n        user_id: str,\n        message_id: str,\n        offset: int\n    ) -> None:\n        \"\"\"حفظ موضع التمرير\"\"\"\n        position = ScrollPosition(\n            conversation_id=conversation_id,\n            user_id=user_id,\n            message_id=message_id,\n            offset=offset,\n            timestamp=datetime.utcnow().isoformat()\n        )\n        self.scroll_positions[f\"{user_id}_{conversation_id}\"] = position\n\n    def get_scroll_position(\n        self,\n        conversation_id: str,\n        user_id: str\n    ) -> Optional[ScrollPosition]:\n        \"\"\"الحصول على موضع التمرير المحفوظ\"\"\"\n        key = f\"{user_id}_{conversation_id}\"\n        return self.scroll_positions.get(key)\n\n    def optimize_attachment(\n        self,\n        attachment: Dict\n    ) -> Dict:\n        \"\"\"تحسين المرفق\"\"\"\n        optimized = attachment.copy()\n        \n        if attachment.get(\"type\") == \"image\":\n            # إضافة معاملات تحسين الصورة\n            if \"url\" in optimized:\n                url = optimized[\"url\"]\n                if \"?\" not in url:\n                    url += \"?\"\n                else:\n                    url += \"&\"\n                optimized[\"url\"] = f\"{url}w=400&q=80&auto=format\"\n        \n        return optimized\n\n    def get_unread_count(\n        self,\n        conversation_id: str,\n        user_id: str\n    ) -> int:\n        \"\"\"الحصول على عدد الرسائل غير المقروءة\"\"\"\n        if conversation_id not in self.messages:\n            return 0\n\n        count = 0\n        for message in self.messages[conversation_id]:\n            if not message.is_read and message.sender_id != user_id:\n                count += 1\n        return count\n\n    def get_stats(self) -> Dict:\n        \"\"\"الحصول على الإحصائيات\"\"\"\n        cache_total = self.stats[\"cache_hits\"] + self.stats[\"cache_misses\"]\n        cache_hit_rate = (\n            (self.stats[\"cache_hits\"] / cache_total * 100)\n            if cache_total > 0\n            else 0\n        )\n        \n        return {\n            **self.stats,\n            \"cache_hit_rate\": f\"{cache_hit_rate:.2f}%\",\n            \"total_conversations\": len(self.messages),\n            \"scroll_positions_saved\": len(self.scroll_positions)\n        }\n\n\n# ============ مثيل مدير الشات ============\n\nchat_manager = OptimizedChatManager()\n\n\n# ============ المسارات (Routes) ============\n\n@app.get(\"/health\")\nasync def health_check():\n    \"\"\"فحص صحة الخدمة\"\"\"\n    return {\n        \"status\": \"healthy\",\n        \"service\": \"optimized-chat-service\",\n        \"version\": \"3.0.0\"\n    }\n\n\n@app.post(\"/messages/{conversation_id}\")\nasync def add_message(\n    conversation_id: str,\n    message_data: Dict,\n    background_tasks: BackgroundTasks\n):\n    \"\"\"إضافة رسالة جديدة\"\"\"\n    try:\n        # تحويل المرفقات\n        attachments = []\n        if message_data.get(\"attachments\"):\n            for att in message_data[\"attachments\"]:\n                attachment = Attachment(\n                    id=att.get(\"id\", str(uuid.uuid4())),\n                    type=AttachmentType(att.get(\"type\", \"file\")),\n                    url=att.get(\"url\", \"\"),\n                    size=att.get(\"size\", 0),\n                    name=att.get(\"name\", \"Unknown\"),\n                    thumbnail=att.get(\"thumbnail\"),\n                    width=att.get(\"width\"),\n                    height=att.get(\"height\"),\n                    duration=att.get(\"duration\")\n                )\n                attachments.append(attachment)\n\n        # إنشاء الرسالة\n        message = OptimizedMessage(\n            id=str(uuid.uuid4()),\n            conversation_id=conversation_id,\n            sender_id=message_data.get(\"sender_id\", \"\"),\n            sender_name=message_data.get(\"sender_name\", \"\"),\n            sender_avatar=message_data.get(\"sender_avatar\", \"\"),\n            content=message_data.get(\"content\", \"\"),\n            timestamp=datetime.utcnow().isoformat(),\n            attachments=attachments\n        )\n\n        # إضافة الرسالة\n        success = chat_manager.add_message(message)\n        \n        if success:\n            return {\n                \"success\": True,\n                \"message_id\": message.id,\n                \"timestamp\": message.timestamp\n            }\n        else:\n            raise HTTPException(status_code=409, detail=\"Duplicate message\")\n    except Exception as e:\n        logger.error(f\"Error adding message: {str(e)}\")\n        raise HTTPException(status_code=500, detail=str(e))\n\n\n@app.get(\"/messages/{conversation_id}\")\nasync def get_messages(\n    conversation_id: str,\n    limit: int = Query(50, ge=1, le=100),\n    offset: int = Query(0, ge=0),\n    use_cache: bool = Query(True)\n):\n    \"\"\"الحصول على الرسائل\"\"\"\n    try:\n        messages = chat_manager.get_messages(\n            conversation_id,\n            limit,\n            offset,\n            use_cache\n        )\n        \n        # تحسين المرفقات\n        for msg in messages:\n            if msg.get(\"attachments\"):\n                msg[\"attachments\"] = [\n                    chat_manager.optimize_attachment(att)\n                    for att in msg[\"attachments\"]\n                ]\n        \n        return {\n            \"success\": True,\n            \"conversation_id\": conversation_id,\n            \"messages\": messages,\n            \"total\": len(chat_manager.messages.get(conversation_id, []))\n        }\n    except Exception as e:\n        logger.error(f\"Error getting messages: {str(e)}\")\n        raise HTTPException(status_code=500, detail=str(e))\n\n\n@app.post(\"/messages/{conversation_id}/{message_id}/read\")\nasync def mark_message_as_read(\n    conversation_id: str,\n    message_id: str,\n    user_id: str = Query(...)\n):\n    \"\"\"تحديد رسالة كمقروءة\"\"\"\n    try:\n        success = chat_manager.mark_as_read(conversation_id, message_id, user_id)\n        if success:\n            return {\"success\": True, \"message\": \"تم تحديد الرسالة كمقروءة\"}\n        else:\n            raise HTTPException(status_code=404, detail=\"الرسالة غير موجودة\")\n    except Exception as e:\n        logger.error(f\"Error marking message as read: {str(e)}\")\n        raise HTTPException(status_code=500, detail=str(e))\n\n\n@app.post(\"/scroll-position/{conversation_id}\")\nasync def save_scroll_position(\n    conversation_id: str,\n    user_id: str = Query(...),\n    message_id: str = Query(...),\n    offset: int = Query(0)\n):\n    \"\"\"حفظ موضع التمرير\"\"\"\n    try:\n        chat_manager.save_scroll_position(\n            conversation_id,\n            user_id,\n            message_id,\n            offset\n        )\n        return {\"success\": True, \"message\": \"تم حفظ موضع التمرير\"}\n    except Exception as e:\n        logger.error(f\"Error saving scroll position: {str(e)}\")\n        raise HTTPException(status_code=500, detail=str(e))\n\n\n@app.get(\"/scroll-position/{conversation_id}\")\nasync def get_scroll_position(\n    conversation_id: str,\n    user_id: str = Query(...)\n):\n    \"\"\"الحصول على موضع التمرير المحفوظ\"\"\"\n    try:\n        position = chat_manager.get_scroll_position(conversation_id, user_id)\n        if position:\n            return {\n                \"success\": True,\n                \"position\": asdict(position)\n            }\n        else:\n            return {\n                \"success\": True,\n                \"position\": None\n            }\n    except Exception as e:\n        logger.error(f\"Error getting scroll position: {str(e)}\")\n        raise HTTPException(status_code=500, detail=str(e))\n\n\n@app.get(\"/unread-count/{conversation_id}\")\nasync def get_unread_count(\n    conversation_id: str,\n    user_id: str = Query(...)\n):\n    \"\"\"الحصول على عدد الرسائل غير المقروءة\"\"\"\n    try:\n        count = chat_manager.get_unread_count(conversation_id, user_id)\n        return {\n            \"success\": True,\n            \"conversation_id\": conversation_id,\n            \"unread_count\": count\n        }\n    except Exception as e:\n        logger.error(f\"Error getting unread count: {str(e)}\")\n        raise HTTPException(status_code=500, detail=str(e))\n\n\n@app.get(\"/stats\")\nasync def get_stats():\n    \"\"\"الحصول على إحصائيات الخدمة\"\"\"\n    return chat_manager.get_stats()\n\n\nif __name__ == \"__main__\":\n    import uvicorn\n    uvicorn.run(app, host=\"0.0.0.0\", port=8005)\n

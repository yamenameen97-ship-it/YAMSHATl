"""\nنظام البحث المتقدم - Advanced Search Service\nيوفر:\n- اقتراحات حية (Live Suggestions)\n- البحث الأخير (Recent Searches)\n- البحث الشائع (Trending Searches)\n- البحث الضبابي (Fuzzy Search)\n- عرض النتائج الذكي (Smart Results Rendering)\n\"\"\"\n\nfrom fastapi import FastAPI, WebSocket, HTTPException, Query\nfrom fastapi.middleware.cors import CORSMiddleware\nimport json\nimport asyncio\nfrom datetime import datetime, timedelta\nfrom typing import Optional, List, Dict, Set\nfrom dataclasses import dataclass, asdict, field\nfrom enum import Enum\nimport logging\nfrom collections import defaultdict, Counter\nimport re\nfrom difflib import SequenceMatcher\nimport uuid\n\n# إعداد السجلات\nlogging.basicConfig(level=logging.INFO)\nlogger = logging.getLogger(__name__)\n\napp = FastAPI(\n    title=\"Yamshat Advanced Search Service\",\n    description=\"نظام بحث متقدم مع اقتراحات حية وبحث ضبابي\",\n    version=\"2.0.0\"\n)\n\n# إضافة CORS\napp.add_middleware(\n    CORSMiddleware,\n    allow_origins=[\"*\"],\n    allow_credentials=True,\n    allow_methods=[\"*\"],\n    allow_headers=[\"*\"],\n)\n\n# ============ تعريفات الأنواع ============\n\nclass ResultType(str, Enum):\n    \"\"\"أنواع نتائج البحث\"\"\"\n    USER = \"user\"\n    POST = \"post\"\n    HASHTAG = \"hashtag\"\n    LOCATION = \"location\"\n    AUDIO = \"audio\"\n\n\nclass SearchCategory(str, Enum):\n    \"\"\"فئات البحث\"\"\"\n    TOP = \"top\"\n    ACCOUNTS = \"accounts\"\n    POSTS = \"posts\"\n    HASHTAGS = \"hashtags\"\n    LOCATIONS = \"locations\"\n    AUDIO = \"audio\"\n\n\n@dataclass\nclass SearchResult:\n    \"\"\"نتيجة البحث\"\"\"\n    id: str = \"\"\n    type: ResultType = ResultType.USER\n    title: str = \"\"\n    description: str = \"\"\n    image: str = \"\"\n    relevance_score: float = 0.0\n    metadata: Dict = field(default_factory=dict)\n    timestamp: str = field(default_factory=lambda: datetime.utcnow().isoformat())\n\n\n@dataclass\nclass SearchSuggestion:\n    \"\"\"اقتراح البحث\"\"\"\n    id: str = field(default_factory=lambda: str(uuid.uuid4()))\n    text: str = \"\"\n    type: str = \"search\"  # search, hashtag, user, location\n    icon: str = \"🔍\"\n    frequency: int = 0  # عدد مرات البحث\n    is_trending: bool = False\n    timestamp: str = field(default_factory=lambda: datetime.utcnow().isoformat())\n\n\n@dataclass\nclass RecentSearch:\n    \"\"\"بحث أخير\"\"\"\n    id: str = field(default_factory=lambda: str(uuid.uuid4()))\n    user_id: str = \"\"\n    query: str = \"\"\n    category: SearchCategory = SearchCategory.TOP\n    timestamp: str = field(default_factory=lambda: datetime.utcnow().isoformat())\n\n\n# ============ محرك البحث المتقدم ============\n\nclass AdvancedSearchEngine:\n    \"\"\"محرك البحث المتقدم\"\"\"\n\n    def __init__(self):\n        # قاعدة البيانات المحاكاة\n        self.users = self._initialize_users()\n        self.posts = self._initialize_posts()\n        self.hashtags = self._initialize_hashtags()\n        self.locations = self._initialize_locations()\n        self.audio = self._initialize_audio()\n\n        # البحث الأخير\n        self.recent_searches: Dict[str, List[RecentSearch]] = defaultdict(list)\n\n        # الاقتراحات والبحث الشائع\n        self.suggestions: List[SearchSuggestion] = []\n        self.trending_searches: List[str] = []\n        self.search_frequency: Counter = Counter()\n\n        # WebSocket للبحث الحي\n        self.active_searches: Dict[str, WebSocket] = {}\n\n    def _initialize_users(self) -> List[Dict]:\n        \"\"\"تهيئة قائمة المستخدمين\"\"\"\n        return [\n            {\"id\": \"user1\", \"name\": \"أحمد محمد\", \"username\": \"ahmed_m\", \"avatar\": \"https://...\"},\n            {\"id\": \"user2\", \"name\": \"فاطمة علي\", \"username\": \"fatima_ali\", \"avatar\": \"https://...\"},\n            {\"id\": \"user3\", \"name\": \"محمود سالم\", \"username\": \"mahmoud_salem\", \"avatar\": \"https://...\"},\n            {\"id\": \"user4\", \"name\": \"سارة خالد\", \"username\": \"sarah_k\", \"avatar\": \"https://...\"},\n            {\"id\": \"user5\", \"name\": \"عمر حسن\", \"username\": \"omar_hassan\", \"avatar\": \"https://...\"},\n        ]\n\n    def _initialize_posts(self) -> List[Dict]:\n        \"\"\"تهيئة قائمة المنشورات\"\"\"\n        return [\n            {\"id\": \"post1\", \"title\": \"رحلتي إلى دبي\", \"content\": \"قضيت أفضل وقت في دبي...\", \"author\": \"ahmed_m\"},\n            {\"id\": \"post2\", \"title\": \"نصائح الطبخ\", \"content\": \"اليوم سأشارك معكم...\", \"author\": \"fatima_ali\"},\n            {\"id\": \"post3\", \"title\": \"تطوير الويب\", \"content\": \"أفضل ممارسات...\", \"author\": \"mahmoud_salem\"},\n        ]\n\n    def _initialize_hashtags(self) -> List[Dict]:\n        \"\"\"تهيئة قائمة الهاشتاجات\"\"\"\n        return [\n            {\"id\": \"tag1\", \"name\": \"#السفر\", \"count\": 15000},\n            {\"id\": \"tag2\", \"name\": \"#الطبخ\", \"count\": 12000},\n            {\"id\": \"tag3\", \"name\": \"#التكنولوجيا\", \"count\": 20000},\n            {\"id\": \"tag4\", \"name\": \"#الرياضة\", \"count\": 18000},\n        ]\n\n    def _initialize_locations(self) -> List[Dict]:\n        \"\"\"تهيئة قائمة المواقع\"\"\"\n        return [\n            {\"id\": \"loc1\", \"name\": \"دبي\", \"country\": \"الإمارات\", \"posts_count\": 50000},\n            {\"id\": \"loc2\", \"name\": \"القاهرة\", \"country\": \"مصر\", \"posts_count\": 75000},\n            {\"id\": \"loc3\", \"name\": \"الرياض\", \"country\": \"السعودية\", \"posts_count\": 45000},\n        ]\n\n    def _initialize_audio(self) -> List[Dict]:\n        \"\"\"تهيئة قائمة الموسيقى\"\"\"\n        return [\n            {\"id\": \"audio1\", \"title\": \"أغنية جميلة\", \"artist\": \"فنان\", \"duration\": 180},\n            {\"id\": \"audio2\", \"title\": \"موسيقى هادئة\", \"artist\": \"موسيقار\", \"duration\": 240},\n        ]\n\n    # ============ البحث الضبابي (Fuzzy Search) ============\n\n    def fuzzy_match(self, query: str, text: str, threshold: float = 0.6) -> float:\n        \"\"\"حساب درجة التطابق الضبابي\"\"\"\n        query_lower = query.lower()\n        text_lower = text.lower()\n\n        # البحث المباشر\n        if query_lower in text_lower:\n            return 1.0\n\n        # البحث الضبابي باستخدام SequenceMatcher\n        ratio = SequenceMatcher(None, query_lower, text_lower).ratio()\n        return ratio if ratio >= threshold else 0.0\n\n    # ============ البحث الحي (Live Search) ============\n\n    def search_live(self, query: str, category: Optional[SearchCategory] = None) -> List[SearchResult]:\n        \"\"\"البحث الحي مع النتائج الفورية\"\"\"\n        if not query or len(query) < 2:\n            return []\n\n        results = []\n\n        # البحث في المستخدمين\n        if not category or category == SearchCategory.ACCOUNTS:\n            for user in self.users:\n                score = max(\n                    self.fuzzy_match(query, user[\"name\"]),\n                    self.fuzzy_match(query, user[\"username\"])\n                )\n                if score > 0:\n                    results.append(SearchResult(\n                        id=user[\"id\"],\n                        type=ResultType.USER,\n                        title=user[\"name\"],\n                        description=f\"@{user['username']}\",\n                        image=user[\"avatar\"],\n                        relevance_score=score\n                    ))\n\n        # البحث في المنشورات\n        if not category or category == SearchCategory.POSTS:\n            for post in self.posts:\n                score = max(\n                    self.fuzzy_match(query, post[\"title\"]),\n                    self.fuzzy_match(query, post[\"content\"])\n                )\n                if score > 0:\n                    results.append(SearchResult(\n                        id=post[\"id\"],\n                        type=ResultType.POST,\n                        title=post[\"title\"],\n                        description=post[\"content\"][:100],\n                        relevance_score=score\n                    ))\n\n        # البحث في الهاشتاجات\n        if not category or category == SearchCategory.HASHTAGS:\n            for hashtag in self.hashtags:\n                score = self.fuzzy_match(query, hashtag[\"name\"])\n                if score > 0:\n                    results.append(SearchResult(\n                        id=hashtag[\"id\"],\n                        type=ResultType.HASHTAG,\n                        title=hashtag[\"name\"],\n                        description=f\"{hashtag['count']:,} منشور\",\n                        relevance_score=score\n                    ))\n\n        # البحث في المواقع\n        if not category or category == SearchCategory.LOCATIONS:\n            for location in self.locations:\n                score = self.fuzzy_match(query, location[\"name\"])\n                if score > 0:\n                    results.append(SearchResult(\n                        id=location[\"id\"],\n                        type=ResultType.LOCATION,\n                        title=location[\"name\"],\n                        description=f\"{location['country']} • {location['posts_count']:,} منشور\",\n                        relevance_score=score\n                    ))\n\n        # ترتيب النتائج حسب الملاءمة\n        results.sort(key=lambda x: x.relevance_score, reverse=True)\n\n        return results[:50]  # إرجاع أفضل 50 نتيجة\n\n    # ============ الاقتراحات (Suggestions) ============\n\n    def get_suggestions(self, query: str, limit: int = 10) -> List[SearchSuggestion]:\n        \"\"\"الحصول على اقتراحات البحث\"\"\"\n        suggestions = []\n\n        # اقتراحات من البحث الشائع\n        for search in self.trending_searches[:limit]:\n            if query.lower() in search.lower():\n                suggestions.append(SearchSuggestion(\n                    text=search,\n                    type=\"search\",\n                    icon=\"🔥\",\n                    frequency=self.search_frequency[search],\n                    is_trending=True\n                ))\n\n        # اقتراحات من الهاشتاجات\n        for hashtag in self.hashtags:\n            if query.lower() in hashtag[\"name\"].lower():\n                suggestions.append(SearchSuggestion(\n                    text=hashtag[\"name\"],\n                    type=\"hashtag\",\n                    icon=\"#\",\n                    frequency=hashtag[\"count\"]\n                ))\n\n        # اقتراحات من المستخدمين\n        for user in self.users:\n            if query.lower() in user[\"name\"].lower() or query.lower() in user[\"username\"].lower():\n                suggestions.append(SearchSuggestion(\n                    text=f\"@{user['username']}\",\n                    type=\"user\",\n                    icon=\"👤\",\n                    frequency=1\n                ))\n\n        return suggestions[:limit]\n\n    # ============ البحث الأخير (Recent Searches) ============\n\n    def add_recent_search(self, user_id: str, query: str, category: SearchCategory = SearchCategory.TOP):\n        \"\"\"إضافة بحث إلى السجل الأخير\"\"\"\n        recent = RecentSearch(\n            user_id=user_id,\n            query=query,\n            category=category\n        )\n        self.recent_searches[user_id].append(recent)\n\n        # الاحتفاظ بآخر 50 بحث فقط\n        if len(self.recent_searches[user_id]) > 50:\n            self.recent_searches[user_id] = self.recent_searches[user_id][-50:]\n\n        # تحديث البحث الشائع\n        self.search_frequency[query] += 1\n        self._update_trending_searches()\n\n    def get_recent_searches(self, user_id: str, limit: int = 10) -> List[RecentSearch]:\n        \"\"\"الحصول على البحث الأخير\"\"\"\n        searches = self.recent_searches.get(user_id, [])\n        return searches[-limit:][::-1]  # إرجاع الأحدث أولاً\n\n    def clear_recent_searches(self, user_id: str):\n        \"\"\"مسح البحث الأخير\"\"\"\n        self.recent_searches[user_id] = []\n\n    # ============ البحث الشائع (Trending Searches) ============\n\n    def _update_trending_searches(self):\n        \"\"\"تحديث قائمة البحث الشائع\"\"\"\n        # الحصول على أكثر 20 بحث تكراراً\n        self.trending_searches = [\n            search for search, _ in self.search_frequency.most_common(20)\n        ]\n\n    def get_trending_searches(self, limit: int = 10) -> List[Dict]:\n        \"\"\"الحصول على البحث الشائع\"\"\"\n        trending = []\n        for i, search in enumerate(self.trending_searches[:limit], 1):\n            trending.append({\n                \"rank\": i,\n                \"query\": search,\n                \"frequency\": self.search_frequency[search],\n                \"icon\": \"🔥\"\n            })\n        return trending\n\n    def remove_recent_search(self, user_id: str, query: str):\n        \"\"\"حذف بحث معين من السجل الأخير\"\"\"\n        self.recent_searches[user_id] = [\n            s for s in self.recent_searches[user_id]\n            if s.query != query\n        ]\n\n\n# ============ مثيل محرك البحث ============\n\nsearch_engine = AdvancedSearchEngine()\n\n\n# ============ المسارات (Routes) ============\n\n@app.get(\"/health\")\nasync def health_check():\n    \"\"\"فحص صحة الخدمة\"\"\"\n    return {\n        \"status\": \"healthy\",\n        \"service\": \"advanced-search-service\",\n        \"version\": \"2.0.0\"\n    }\n\n\n@app.get(\"/search\")\nasync def search(\n    q: str = Query(..., min_length=2),\n    category: Optional[SearchCategory] = None,\n    limit: int = Query(50, ge=1, le=100)\n):\n    \"\"\"البحث الحي\"\"\"\n    try:\n        results = search_engine.search_live(q, category)\n        return {\n            \"success\": True,\n            \"query\": q,\n            \"category\": category,\n            \"results\": [asdict(r) for r in results[:limit]],\n            \"total\": len(results)\n        }\n    except Exception as e:\n        logger.error(f\"Search error: {str(e)}\")\n        raise HTTPException(status_code=500, detail=str(e))\n\n\n@app.get(\"/suggestions\")\nasync def get_suggestions(\n    q: str = Query(..., min_length=1),\n    limit: int = Query(10, ge=1, le=50)\n):\n    \"\"\"الحصول على الاقتراحات\"\"\"\n    try:\n        suggestions = search_engine.get_suggestions(q, limit)\n        return {\n            \"success\": True,\n            \"query\": q,\n            \"suggestions\": [asdict(s) for s in suggestions]\n        }\n    except Exception as e:\n        logger.error(f\"Suggestions error: {str(e)}\")\n        raise HTTPException(status_code=500, detail=str(e))\n\n\n@app.get(\"/recent/{user_id}\")\nasync def get_recent_searches(\n    user_id: str,\n    limit: int = Query(10, ge=1, le=50)\n):\n    \"\"\"الحصول على البحث الأخير\"\"\"\n    try:\n        searches = search_engine.get_recent_searches(user_id, limit)\n        return {\n            \"success\": True,\n            \"user_id\": user_id,\n            \"searches\": [asdict(s) for s in searches]\n        }\n    except Exception as e:\n        logger.error(f\"Recent searches error: {str(e)}\")\n        raise HTTPException(status_code=500, detail=str(e))\n\n\n@app.post(\"/recent/{user_id}\")\nasync def add_recent_search(\n    user_id: str,\n    query: str = Query(...),\n    category: SearchCategory = SearchCategory.TOP\n):\n    \"\"\"إضافة بحث إلى السجل الأخير\"\"\"\n    try:\n        search_engine.add_recent_search(user_id, query, category)\n        return {\n            \"success\": True,\n            \"message\": \"تم إضافة البحث إلى السجل\"\n        }\n    except Exception as e:\n        logger.error(f\"Add recent search error: {str(e)}\")\n        raise HTTPException(status_code=500, detail=str(e))\n\n\n@app.delete(\"/recent/{user_id}\")\nasync def clear_recent_searches(user_id: str):\n    \"\"\"مسح البحث الأخير\"\"\"\n    try:\n        search_engine.clear_recent_searches(user_id)\n        return {\n            \"success\": True,\n            \"message\": \"تم مسح البحث الأخير\"\n        }\n    except Exception as e:\n        logger.error(f\"Clear recent searches error: {str(e)}\")\n        raise HTTPException(status_code=500, detail=str(e))\n\n\n@app.delete(\"/recent/{user_id}/{query}\")\nasync def remove_recent_search(user_id: str, query: str):\n    \"\"\"حذف بحث معين\"\"\"\n    try:\n        search_engine.remove_recent_search(user_id, query)\n        return {\n            \"success\": True,\n            \"message\": \"تم حذف البحث\"\n        }\n    except Exception as e:\n        logger.error(f\"Remove recent search error: {str(e)}\")\n        raise HTTPException(status_code=500, detail=str(e))\n\n\n@app.get(\"/trending\")\nasync def get_trending_searches(\n    limit: int = Query(10, ge=1, le=50)\n):\n    \"\"\"الحصول على البحث الشائع\"\"\"\n    try:\n        trending = search_engine.get_trending_searches(limit)\n        return {\n            \"success\": True,\n            \"trending\": trending\n        }\n    except Exception as e:\n        logger.error(f\"Trending searches error: {str(e)}\")\n        raise HTTPException(status_code=500, detail=str(e))\n\n\n@app.get(\"/stats\")\nasync def get_stats():\n    \"\"\"إحصائيات الخدمة\"\"\"\n    return {\n        \"total_users\": len(search_engine.users),\n        \"total_posts\": len(search_engine.posts),\n        \"total_hashtags\": len(search_engine.hashtags),\n        \"total_locations\": len(search_engine.locations),\n        \"total_searches_tracked\": len(search_engine.search_frequency),\n        \"trending_count\": len(search_engine.trending_searches)\n    }\n\n\nif __name__ == \"__main__\":\n    import uvicorn\n    uvicorn.run(app, host=\"0.0.0.0\", port=8004)\n
